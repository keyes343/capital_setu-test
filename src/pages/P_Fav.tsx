import React from 'react';
import { s, reuse } from './incoming';

interface FavProps {}

const Fav: React.FC<FavProps> = (p) => {
    return (
        <s.Box>
            fav
        </s.Box>
    );
};

export default Fav;


// ------------------

